# ============================================================
# 📄 파일명: ai_modules/configs/baseline.yaml
# 📘 목적: 학습/추론에 공통으로 쓰는 기본 설정임.
# ============================================================

# --- 데이터 경로 ---
image_root: "/path/to/TS_81_단일crop128"   # 예: .../원천데이터/단일경구약제 5000종/TS_81_단일crop128
label_root: "/path/to/TL_81_단일"         # 예: .../라벨링데이터/단일경구약제 5000종/TL_81_단일
label_key: "dl_name"

# --- 학습 하이퍼파라미터 ---
img_size: 128
batch_size: 32
epochs: 5
lr: 0.001
seed: 42

# --- 결과 저장 ---
save_dir: "runs/exp001"

# --- (선택) 통합 추론 설정 ---
predict:
  img_size: 64
  idx2label_json: "/path/to/matched_all.json"
  num_classes: null        # null이면 idx2label 길이로 추정함
  ask_llm: true
  openai_model: "gpt-4"
